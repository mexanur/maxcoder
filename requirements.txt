# ── Web framework ──────────────────────────────────────────────────────────────
fastapi>=0.115.2
uvicorn[standard]==0.30.6
httpx==0.27.2
pydantic==2.9.2

# ── LLM inference ──────────────────────────────────────────────────────────────
# Direct llama.cpp bindings — no Ollama daemon needed.
# For CUDA GPU support install with:
#   pip install --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121 llama-cpp-python==0.3.4
# CPU-only fallback: pip install llama-cpp-python==0.3.4
llama-cpp-python==0.3.4
python-multipart==0.0.12
python-dotenv==1.0.1

# ── RAG / Embeddings ───────────────────────────────────────────────────────────
chromadb==0.5.5
sentence-transformers==3.1.1
pypdf==5.0.0
torch==2.4.1
transformers==4.44.2

# ── File parsing & generation ───────────────────────────────────────────────────
python-docx>=1.1.2
openpyxl>=3.1.5
fpdf2>=2.8.0

# ── Web Search ─────────────────────────────────────────────────────────────────
duckduckgo-search==6.3.7
